class Coffee {
  String email, image, id, name, price, quantity, rating;
  Coffee({
    this.email,
    this.image, 
    this.id, 
    this.name, 
    this.price,
    this.quantity,
    this.rating});
}